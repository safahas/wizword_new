# OpenRouter API Configuration
OPENROUTER_API_KEY=your_api_key_here
USE_CLOUD_STORAGE=false

# Game Configuration
DEFAULT_MODEL=mistralai/mistral-small-24b-instruct-2501:free
HTTP_REFERER=https://github.com/cursor
X_TITLE=Word Guess Game

# Optional AWS Configuration (only if USE_CLOUD_STORAGE=true)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Optional SMTP Configuration (for email alerts)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Rate Limiting Configuration
REQUESTS_PER_MINUTE=30
REQUESTS_PER_HOUR=500
COOLDOWN_PERIOD=60
RETRY_AFTER=5

# Quota Warning Thresholds (percentage)
WARNING_THRESHOLD=20
CRITICAL_THRESHOLD=10 